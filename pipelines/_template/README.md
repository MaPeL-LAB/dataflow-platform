# Pipeline Template

Copy this directory to `pipelines/<new_name>/`, replace the placeholder implementation, add tests, and register the new pipeline in `config/pipelines.yml`.

The entrypoint contract is `run_pipeline_cli(args, pipeline, repo_root)`.
